server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:6541/ewm-main
    username: ewm
    password: ewm
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    serialization:
      write-dates-as-timestamps: false

stats:
  server-url: http://localhost:9090


